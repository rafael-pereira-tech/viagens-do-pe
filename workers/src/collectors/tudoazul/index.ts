/**
 * BE-4: TudoAzul / AZUL collector (PET→VCP and PET→POA, miles + BRL).
 *
 * Sources: `tudoazul_web` (points) and `voeazul` (full cash). `program` stays `tudoazul`.
 */
export { createTudoAzulCollector, tudoAzulCollector } from './collector';
